-- ============================================================
--  Re:Mind — After the Fall (RMATF)
--  Author: Seppel
-- ============================================================

-- 000_RMATF_Boot.lua
-- This is intentionally at shared root so it always autoloads.
-- It only pulls in RMATF/Init.

require("RMATF/Init")
